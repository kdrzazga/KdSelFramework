package org.kd.selframework.widgets;

import org.kd.selframework.pageobjects.Widget;
import org.openqa.selenium.WebDriver;

class WO_Banner extends Widget {
    public WO_Banner(WebDriver driver) {
        super(driver);
    }
}
